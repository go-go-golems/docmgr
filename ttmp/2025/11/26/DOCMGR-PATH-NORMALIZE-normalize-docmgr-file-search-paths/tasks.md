# Tasks

## TODO

- [x] Capture current relate/search path handling behavior in a reference doc
- [x] Draft canonical path normalization & fuzzy search design
- [x] Implement `internal/paths` resolver (repo/doc/ttmp anchors + cleaning)
- [x] Update relate/search commands to use canonical paths + suffix matching
- [x] Add regression tests and docs for the new normalization behavior (unit tests + scenario playbook)
- [x] Run `test-scenarios/testing-doc-manager/run-all.sh` to ensure baseline steps still pass
- [x] Run `test-scenarios/testing-doc-manager/14-path-normalization.sh` and capture output in ticket log

