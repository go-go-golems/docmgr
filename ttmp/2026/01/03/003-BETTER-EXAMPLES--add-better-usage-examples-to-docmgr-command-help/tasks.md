# Tasks

## TODO

- [ ] Inventory every `docmgr` command and flags (from `docmgr --help` and per-command `--help`)
- [ ] Add 2 real examples to each command long help (`cmds.WithLong(...)`)
- [ ] Execute each example “for real” against this ticket or safe read-only commands
- [ ] Update ticket changelog with what changed + commit hashes
- [ ] Run `go test ./... -count=1`
