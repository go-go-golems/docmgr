# Tasks

## TODO

- [ ] Add tasks here

- [ ] Define shared field-level validators (field/severity/hint/check) and emit FrontmatterSchema taxonomies from doctor + validate frontmatter
- [ ] Add hints to FrontmatterSchemaContext and render hints in FrontmatterSchemaRule
- [ ] Wire validators into doctor (workspace + --doc) and validate frontmatter; keep vocab warnings separate
- [ ] Docs/tests: update diagnostics/CLI help to mention schema validators; add unit tests for validator table; update smokes if needed
